#include "Pods-Parse-OSX-SDK.xcconfig"
FRAMEWORK_SEARCH_PATHS = ${PODS_PARSE_OSX_SDK_FRAMEWORK_SEARCH_PATHS}
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Parse-OSX-SDK" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Parse-OSX-SDK"
OTHER_LDFLAGS = -ObjC ${PODS_PARSE_OSX_SDK_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}